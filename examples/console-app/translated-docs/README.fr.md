---
translation_last_updated: '2026-07-10T20:45:33.868Z'
source_file_mtime: '2026-07-05T22:05:35.671Z'
source_file_hash: dcf4013f15bc8b51e6c8c55f7963ed55c9f1ce31a17b3dfd87ba1ff877e62c94
translation_language: fr
source_file_path: README.md
translation_models:
  - openai/gpt-4o-mini
  - qwen/qwen3-235b-a22b-2507
  - qwen/qwen3.6-plus
---
# Exemple d'Application Console

<small>**Lire dans d'autres langues :** </small>
<small id="lang-list">[English (UK)](../README.md) · [Español](./README.es.md) · [Français](./README.fr.md) · [Deutsch](./README.de.md) · [Português (Brasil)](./README.pt-BR.md)</small>

Cet exemple montre comment utiliser `ai-i18n-tools` pour internationaliser une application console Node.js.

## Prérequis

- Node.js >= 22.16.0
- [pnpm](https://pnpm.io/)
- Une clé API [OpenRouter](https://openrouter.ai) (pour générer les traductions)

## Installation

### Essayez cet exemple indépendamment

Copiez uniquement ce dossier d'exemple et installez `ai-i18n-tools` depuis npm :

```bash
npx degit wsj-br/ai-i18n-tools/examples/console-app console-app
cd console-app
pnpm install
```

### Depuis le dépôt complet ai-i18n-tools

Utilisez ceci lorsque vous avez cloné le **tout** [ai-i18n-tools](https://github.com/wsj-br/ai-i18n-tools) dépôt (pas seulement ce dossier avec degit). Exécutez `pnpm install` depuis la **racine du dépôt**. L'entrée de l'espace de travail [`overrides`](../../pnpm-workspace.yaml) (`ai-i18n-tools: workspace:*`) force `ai-i18n-tools` à la copie locale de l'espace de travail même si cet exemple déclare `"ai-i18n-tools": "^1.7.2"`. Aucune étape de lien séparée n'est nécessaire — reconstruisez la bibliothèque (`pnpm run build` à la racine du dépôt) chaque fois que vous modifiez des fichiers source et l'exemple récupérera automatiquement le `dist/` mis à jour.

```bash
# From the repository root
pnpm install
pnpm run build
```

## Utilisation

Exécutez les commandes ci-dessous depuis le répertoire racine de cet exemple. Après `npx degit …`, vous vous y trouvez déjà (`cd console-app`). Depuis le dépôt complet [ai-i18n-tools](https://github.com/wsj-br/ai-i18n-tools), utilisez plutôt `cd examples/console-app`.

Exécutez l'application en anglais (par défaut) :

```bash
pnpm start
```

Utilisez l'option `--locale` pour afficher la sortie dans une autre langue :

```bash
pnpm run start:es    # Spanish
pnpm run start:fr    # French
pnpm run start:de    # German
pnpm run start:pt-BR # Portuguese (Brazil)
```

Ou passez le drapeau directement :

```bash
node src/main.js --locale fr
```

## Langues prises en charge

| Code    | Language            |
| ------- | ------------------- |
| `en`    | Anglais (par défaut)   |
| `es`    | Espagnol             |
| `fr`    | Français              |
| `de`    | Allemand              |
| `pt-BR` | Portugais (Brésil) |

## Flux de travail

### 1. Extraire les chaînes de l'interface utilisateur

Analyse `src/` pour les appels `t()` et met à jour `locales/strings.json` :

```bash
pnpm run i18n:extract
```

### 2. Traduire

Génère les fichiers JSON de locale dans `locales/` et les documents markdown traduits dans `translated-docs/` :

```bash
export OPENROUTER_API_KEY=your_key_here
pnpm run i18n:translate
```

Vous pouvez également exécuter `i18n:sync` pour extraire et traduire les chaînes de l'interface utilisateur en une seule étape :

```bash
pnpm run i18n:sync
```

### 3. Exécuter l'application

```bash
pnpm run start:es
```

### Vider le cache et retraduire

`i18n:clean` supprime uniquement `.translation-cache/` (il ne supprime **pas** les fichiers `locales/` ou `translated-docs/` validés).

```bash
pnpm run i18n:clean && pnpm run i18n:extract && pnpm run i18n:translate
```

## Structure du projet

```text
console-app/
├── ai-i18n-tools.config.json   # Tool configuration
├── src/
│   ├── main.js                 # Application entry point
│   └── i18n.js                 # i18next setup; locale loaders derived from locales/ui-languages.json
└── locales/
    ├── ui-languages.json       # Supported language definitions
    ├── strings.json            # Master catalog (regenerated by extract; committed)
    ├── es.json                 # Spanish flat strings (committed)
    ├── fr.json                 # French flat strings (committed)
    ├── de.json                 # German flat strings (committed)
    └── pt-BR.json              # Portuguese (Brazil) flat strings (committed)
```

Le répertoire `translated-docs/` est validé dans Git avec les README traduits afin que vous puissiez les lire sans exécuter la traduction ; relancez `i18n:translate` après avoir modifié la source en anglais.
