---
translation_last_updated: '2026-09-14T19:52:38.128Z'
source_file_mtime: '2026-09-14T19:41:20.670Z'
source_file_hash: 6dd907fbf677c7f74b4b338263c315239bdf5b35ae40fe1b462c93766b0a54e9
translation_language: es
source_file_path: README.md
translation_models:
  - google/gemini-2.5-flash
  - qwen/qwen3-235b-a22b-2507
  - qwen/qwen3.6-plus
---
# Ejemplo de Aplicación de Consola

<small>**Leer en otros idiomas:** </small>
<small id="lang-list">[English (UK)](../README.md) · [Español](./README.es.md) · [Français](./README.fr.md) · [Deutsch](./README.de.md) · [Português (Brasil)](./README.pt-BR.md)</small>

Este ejemplo demuestra cómo usar `ai-i18n-tools` para internacionalizar una aplicación de consola Node.js.

## Requisitos

- Node.js >= 22.16.0
- [pnpm](https://pnpm.io/)
- Una clave de API de [OpenRouter](https://openrouter.ai) (para generar traducciones)

## Instalación

### Prueba este ejemplo por separado

Copie solo esta carpeta de ejemplo e instale `ai-i18n-tools` desde npm:

```bash
npx degit wsj-br/ai-i18n-tools/examples/console-app console-app
cd console-app
pnpm install
```

### Desde el repositorio completo de ai-i18n-tools

Utilice esto cuando haya clonado el repositorio **completo** de [ai-i18n-tools](https://github.com/wsj-br/ai-i18n-tools) (no solo esta carpeta con degit). Ejecute `pnpm install` desde la **raíz del repositorio**. La entrada del [`overrides`](../../pnpm-workspace.yaml) espacio de trabajo (`ai-i18n-tools: workspace:*`) fuerza a `ai-i18n-tools` a la copia local del espacio de trabajo, aunque este ejemplo declare un rango de intercalación publicado. No se necesita un paso de enlace separado: reconstruya la biblioteca (`pnpm run build` en la raíz del repositorio) cada vez que cambie los archivos de origen y el ejemplo tomará automáticamente el `dist/` actualizado.

```bash
# From the repository root
pnpm install
pnpm run build
```

## Uso

Ejecute los comandos siguientes desde el directorio raíz de este ejemplo. Tras `npx degit …` ya se encuentra allí (`cd console-app`). Desde el repositorio completo de [ai-i18n-tools](https://github.com/wsj-br/ai-i18n-tools), use `cd examples/console-app` en su lugar.

Ejecuta la aplicación en inglés (predeterminado):

```bash
pnpm start
```

Usa la opción `--locale` para mostrar la salida en un idioma diferente:

```bash
pnpm run start:es    # Spanish
pnpm run start:fr    # French
pnpm run start:de    # German
pnpm run start:pt-BR # Portuguese (Brazil)
```

O pasa la bandera directamente:

```bash
node src/main.js --locale fr
```

## Idiomas Soportados

| Code    | Language            |
| ------- | ------------------- |
| `en`    | Inglés (predeterminado)   |
| `es`    | Español             |
| `fr`    | Francés              |
| `de`    | Alemán              |
| `pt-BR` | Portugués (Brasil) |

## Flujo de Trabajo

### 1. Extraer cadenas de la interfaz de usuario

Escanea `src/` en busca de llamadas `t()` y actualiza `locales/strings.json`:

```bash
pnpm run i18n:extract
```

### 2. Traducir

Genera archivos JSON de localización en `locales/` y documentos markdown traducidos en `translated-docs/`:

```bash
export OPENROUTER_API_KEY=your_key_here
pnpm run i18n:translate
```

También puedes ejecutar `i18n:sync` para extraer y traducir las cadenas de la interfaz de usuario en un solo paso:

```bash
pnpm run i18n:sync
```

### 3. Ejecutar la aplicación

```bash
pnpm run start:es
```

### Borrar caché y retraducir

`i18n:clean` solo elimina `.translation-cache/` (**no** elimina los directorios `locales/` o `translated-docs/` confirmados en git).

```bash
pnpm run i18n:clean && pnpm run i18n:extract && pnpm run i18n:translate
```

## Estructura del proyecto

```text
console-app/
├── ai-i18n-tools.config.json   # Tool configuration
├── src/
│   ├── main.js                 # Application entry point
│   └── i18n.js                 # i18next setup; locale loaders derived from locales/ui-languages.json
└── locales/
    ├── ui-languages.json       # Supported language definitions
    ├── strings.json            # Master catalog (regenerated by extract; committed)
    ├── es.json                 # Spanish flat strings (committed)
    ├── fr.json                 # French flat strings (committed)
    ├── de.json                 # German flat strings (committed)
    └── pt-BR.json              # Portuguese (Brazil) flat strings (committed)
```

El directorio `translated-docs/` se confirma en git con los README traducidos para que puedas leerlos sin ejecutar la traducción; vuelve a ejecutar `i18n:translate` después de cambiar la fuente en inglés.
