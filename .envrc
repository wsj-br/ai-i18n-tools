[ -f .env ]       && source .env
[ -f .env.local ] && source .env.local

# `bin/ai-i18n-tools` (no .mjs) so the bare command works in this checkout.
# `node_modules/.bin` for other local tools (eslint, vitest, …).
PATH_add node_modules/.bin
PATH_add bin

