<a id="svg-troubleshooting"></a>
# SVG 故障排除

另请参阅[图片和屏幕截图故障排除](/guide/images-and-screenshots/troubleshooting)。

- **SVG 源文件和输出文件在同一目录中** — 将 `svg.sourcePath` 和 `svg.outputDir` 分开。
- **并置 SVG 的绝对 Docusaurus 静态 URL** — 从一开始就使用相对 `../assets/` 路径。
